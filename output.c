#include <stdio.h>
#include <stdbool.h>

int multiplyAndAdd(int a, int b) {
    int product = (a * b);
    int result = (product + 10);
    return result;
}

int main() {
    bool x = true;
    int y = 4;
    (y - 9);
    y = 10;
    printf("%d\n", x);
    int answer = multiplyAndAdd(5, 3);
    printf("%d\n", answer);
    return 0;
}
